

<form action="agregar" method="POST" class="my-4">
    <div class="row">
        <?php /*
        <div class="col-3">
            <div class="form-group">
<           ?php foreach ($productos as $producto) ?>
            <select class="form-control" name="categoria">
                    <tr>
                        <td> 
                            <option value = "<?php $producto->id_categoria ?>"> 
                            <?php echo $producto->categoria ?> </option> 
                        </td>
                    </tr> 
                </select>
            </div>
        </div>
        */ ?>
        <?php /*
        <div class="form-group">
                <label>Producto</label>
                <div class="form-group">
                    <input required name="producto" type="text" class="form-control">
                </div>
        </div>
        */ ?>
        <div class="form-group">
            <label>Marcas</label>
            <?php foreach ($productos as $producto) ?>
            <select class="form-control" name="marca">
                <tr>
                    <td> 
                        <option value = "<?php $producto->marca ?>"> 
                        <?php echo $producto->marca ?> </option> 
                        
                    </td>
                </tr> 
            </select>
        </div>
        <?php /*
        <div class="form-group">
            <label>Precio</label>
            <input required name="precio" type="float" class="form-control">
        </div>
        <div class="form-group">
            <label>Talle</label>
            <input required name="talle" type="float" class="form-control">
        </div>
        */ ?>
    </div>
    <button type="submit" class="btn btn-primary mt-2">Agregar</button>
</form>

